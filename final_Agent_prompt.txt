Build the complete responsive React frontend for the BrainScan AI Brain MRI Classification application.

I am attaching a reference UI image. Use the attached image as the primary visual reference and reproduce its design language, layout, spacing, typography, colors, cards, upload area, responsive behavior, and overall premium medical-AI appearance.

The application must be fully functional, not a static mockup.

1. Tech Stack

Use:

React
Vite
JavaScript or TypeScript
Modern CSS
fetch or Axios
Lucide React or another lightweight icon library

Do not add unnecessary libraries.

2. IMPORTANT — BACKEND API INTEGRATION

The FastAPI backend is already implemented separately.

Do not create fake/mock API responses.

The frontend must properly use the real backend APIs.

Backend endpoints:

GET  /health
POST /api/chat
POST /api/validate-mri
POST /api/analyze

The main frontend MRI workflow MUST use:

POST /api/analyze

The frontend must send the selected file using:

Content-Type: multipart/form-data

with the exact field name:

file

Example request logic:

const formData = new FormData();
formData.append("file", selectedFile);

const response = await fetch(`${API_BASE_URL}/api/analyze`, {
  method: "POST",
  body: formData,
});

Do not manually set the Content-Type header when using FormData; allow the browser to generate the multipart boundary.

Create a dedicated API service/module such as:

src/services/api.js

All backend communication should be centralized there.

Do not scatter API URLs and fetch logic throughout components.

3. Backend Base URL

Use an environment variable:

VITE_API_BASE_URL

Example:

VITE_API_BASE_URL=http://localhost:8000

Never hard-code:

http://localhost:8000

throughout the application.

Create .env.example.

4. Use the REAL /api/analyze Response

The frontend must consume the actual backend response.

Expected successful structure:

{
  "success": true,
  "valid_mri": true,
  "prediction": {
    "class_id": 1,
    "class_name": "Glioma",
    "confidence": 0.94
  },
  "probabilities": {
    "No Tumor": 0.01,
    "Glioma": 0.94,
    "Meningioma": 0.03,
    "Pituitary Tumor": 0.02
  },
  "explanation": "..."
}

Do NOT calculate or invent classification data in the frontend.

Use exactly what the backend returns.

The backend CNN remains authoritative for classification.

Gemini explanation must be displayed from:

response.explanation

Do not generate your own explanation in the frontend.

5. Handle Backend Responses Correctly

Implement proper handling for:

Successful analysis

Display:

prediction
confidence
four-class probabilities
Gemini explanation
Invalid MRI

Example:

{
  "success": false,
  "valid_mri": false,
  "message": "The uploaded file is not a suitable brain MRI."
}

Display the backend message.

Do NOT display CNN classification information when valid_mri is false.

HTTP errors

Gracefully handle:

400
413
422
500
502
503
network failure
timeout

Never display raw stack traces or raw backend error objects.

Show a clear user-friendly message.

6. API Error Handling

Create a reusable API helper that:

checks response.ok
safely parses JSON
handles non-JSON responses
returns meaningful errors
handles network failures
handles unavailable backend

Example user-facing message:

Unable to connect to the analysis service. Please try again.

Do not expose backend implementation details.

7. Optional /health Usage

The frontend may use:

GET /health

to determine backend availability where useful.

Do not repeatedly poll it unnecessarily.

Do not block the entire application from rendering just because /health is unavailable.

8. API Architecture

Recommended:

src/
├── services/
│   └── api.js
├── components/
│   ├── Header
│   ├── Hero
│   ├── FeatureHighlights
│   ├── UploadCard
│   ├── UploadDropzone
│   ├── CameraButton
│   ├── GalleryButton
│   ├── AnalyzeButton
│   ├── LoadingState
│   ├── ResultCard
│   ├── ConfidenceBar
│   ├── ProbabilityList
│   └── ErrorCard
├── hooks/
├── App.jsx
├── main.jsx
└── styles/

Keep the API layer independent from UI components.

9. Core UI

Reproduce the attached reference design.

The page should have:

Header
BrainScan AI logo/icon
"BrainScan AI"
theme toggle

No unnecessary navigation.

Do NOT add:

Ask a question
About
Contact
Disclaimer
Login
Signup
User profile
Dashboard navigation
10. Hero Section

Use the reference composition.

Label:

AI FOR A HEALTHIER TOMORROW

Heading:

Understand
Your Brain Health
with AI

Make AI blue.

Supporting text:

Upload a brain MRI and get AI-powered insights using advanced deep learning and Gemini. Fast, private and reliable.

Feature indicators:

Fast Response
Privacy First
Simple & Reliable

Use the brain visual from the reference as visual inspiration.

11. Upload Area

Create the large rounded upload card shown in the reference.

Display:

Drag & drop a brain MRI image here

Then:

or

Then:

Choose File

Supporting text:

Supports: JPG, JPEG, PNG, PDF • Max size: 20 MB

Supported formats:

JPG
JPEG
PNG
PDF

Maximum:

20 MB
12. Drag and Drop

Implement real drag-and-drop.

Required behavior:

drag over state
drop file
invalid file handling
file selection
remove/change file
visual feedback

Do not merely create a decorative dropzone.

13. Take Photo

Implement the camera action.

Use a mobile-friendly camera input such as:

<input
  type="file"
  accept="image/*"
  capture="environment"
/>

The "Take a Photo" action must actually open the device camera where supported.

14. Gallery

"Choose from Gallery" must open the device file picker.

Support the required image/file formats.

15. File Validation

Before calling /api/analyze, validate:

a file exists
supported extension
maximum 20 MB

Show a clear frontend error for invalid input.

The backend remains responsible for the actual MRI validation.

Do not attempt to determine whether an image is a brain MRI using frontend logic.

16. Preview

For JPG/JPEG/PNG:

Show an image preview.

For PDF:

Show a clean file preview/card containing:

filename
file type
file size

Do not pretend a PDF is an image.

Provide remove/change functionality.

17. Analyze Button

Large full-width button:

Analyze →

Rules:

No file

Disabled.

File selected

Enabled.

During analysis

Disabled with loading state:

Analyzing...

Prevent duplicate API submissions.

After error

Allow retry.

18. Loading UI

While /api/analyze is running:

Show a polished loading experience.

For example:

Analyzing MRI...

Supporting message:

Validating the image and generating AI-powered insights.

Use subtle animation.

Do not make the interface appear frozen.

19. Result UI

After successful /api/analyze response, transition to a polished results section.

Display:

Prediction

Example:

Glioma
Confidence

Example:

94%

Do not call it:

medical certainty
cancer probability
diagnosis

It is only the CNN classification confidence.

20. Probability Display

Display all four backend-provided probabilities:

No Tumor
Glioma
Meningioma
Pituitary Tumor

Use clean progress bars or equivalent visual representation.

The values must come directly from:

response.probabilities

Do not calculate alternate scores.

21. Gemini Explanation

Display the exact backend-provided explanation:

response.explanation

in a clean card.

Do not modify the factual meaning.

Do not create additional medical claims.

22. Invalid MRI UI

If:

success === false
valid_mri === false

display a clear error/information card using the backend response.

Example:

The uploaded file is not a suitable brain MRI.

Provide:

Choose another file

Do not show a prediction.

23. Start New Analysis

After displaying a result, provide a clear action such as:

Analyze Another MRI

This should reset:

selectedFile
preview
analysisResult
error
loading state

without refreshing the entire page.

24. Responsive Design

This is a critical requirement.

The frontend must be fully responsive.

Support:

320px+
375px+
425px+
768px+
1024px+
1280px+
1440px+
1920px+

The reference image demonstrates both desktop and mobile behavior.

Do NOT simply shrink the desktop design.

Build proper responsive layouts.

Mobile
compact header
correctly scaled heading
brain visual remains proportional
feature items wrap
upload card fits viewport
buttons are touch-friendly
no horizontal scrolling
result cards fit narrow screens
readable font sizes
appropriate vertical spacing
Tablet

Adapt hero layout and upload controls appropriately.

Desktop

Maintain the spacious composition shown in the reference.

25. Visual Style

Maintain the reference style:

white
very light blue
blue accents
dark navy text
soft gradients
subtle shadows
rounded cards
premium spacing
clean medical-AI aesthetic

Avoid:

excessive gradients
excessive animations
clutter
dark dashboard styling
excessive cards
unnecessary charts
oversized text on mobile
26. Theme Toggle

Implement the theme toggle shown in the reference.

If dark mode is supported, ensure the entire UI actually adapts.

If necessary, persist the theme locally.

Do not let theme changes break readability or accessibility.

27. Accessibility

Use:

semantic HTML
labels
accessible buttons
keyboard navigation
visible focus states
proper contrast
useful ARIA attributes

Do not rely only on color to communicate errors or states.

28. State Management

Maintain clean application state for at least:

selectedFile
preview
isAnalyzing
analysisResult
error

Reset state correctly.

Do not introduce unnecessary global state management.

29. No Fake Data

STRICTLY DO NOT:

hard-code a prediction
hard-code confidence
hard-code probabilities
mock Gemini responses
mock /api/analyze
create fake API fallback data

Everything displayed as analysis output must originate from the real backend response.

30. Privacy

Frontend must not:

upload files anywhere except the configured FastAPI backend
save MRI files to permanent browser storage
create client-side history
maintain user accounts
maintain analysis history

The backend handles temporary processing.

31. No Unnecessary Features

Do NOT add:

database
authentication
user history
chat interface
separate question page
admin panel
analytics
unnecessary navigation
fake AI features

Keep the application focused on MRI analysis.

32. Production-Quality Implementation

Use:

reusable components
clean naming
modular API layer
responsive CSS
clear state transitions
maintainable folder structure
proper loading/error handling

Do not put the entire application into one giant React component.

33. Important Backend Contract

Treat the backend API as the source of truth.

Do not modify backend assumptions.

The frontend must work with:

GET  /health
POST /api/chat
POST /api/validate-mri
POST /api/analyze

The primary workflow is:

User selects MRI/PDF
        ↓
Frontend validates file
        ↓
POST /api/analyze
        ↓
Backend validates MRI with Gemini
        ↓
Backend runs SmallCNN
        ↓
Backend gets Gemini explanation
        ↓
Backend returns JSON
        ↓
Frontend renders result
34. No Testing

STRICTLY DO NOT:

run the frontend
run the development server
call the backend APIs
call /health
call /api/analyze
call Gemini
upload files for testing
create test files
run automated tests
perform browser testing
claim that the frontend was tested or verified

Implement the code only.

Static code consistency is sufficient.

I will perform testing manually.

35. Final Deliverable

Create the complete React frontend from the attached reference image.

The final result must be:

visually faithful to the reference
responsive on desktop/tablet/mobile
connected to the real FastAPI backend
using /api/analyze correctly
using real backend response data
handling loading/errors properly
supporting drag/drop
supporting camera
supporting gallery
supporting JPG/JPEG/PNG/PDF
enforcing 20 MB client-side limit
free of mock data
cleanly componentized

Do not modify the backend.

Do not test anything.

At the end, report only:

Files created/modified
Frontend structure
Backend API integration implemented
Environment variable required
Manual steps for me to run it